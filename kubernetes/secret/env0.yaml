version: 1

deploy:
  steps:
    setupVariables:
      after: &login-k8s
        - mkdir -p ~/.kube
        - awsv2 eks update-kubeconfig --name $CLUSTER_NAME --region us-west-2 

destroy:
  steps:
    setupVariables:
      after: *login-k8s